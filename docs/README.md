# docs/ — Thư mục chứa file nội dung gốc

Đặt file nội dung gốc của website vào đây:

- `Nội dung Website IVP_1 (1).docx`

File `.docx` hiện đang nằm ở thư mục gốc của project (`../`). Vui lòng **di chuyển** (cut → paste) file đó vào thư mục `docs/` này để gọn gàng theo đúng cấu trúc mô tả trong `README.md`.

> Ghi chú: file `.docx` nặng ~37MB nên cần di chuyển thủ công.
